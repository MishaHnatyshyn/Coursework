#pragma once
#include <fstream>
#include <iostream>
#include <ctime>
#include <vector>
#include <string>
using namespace std;

vector<int> generate_random_array(int, int, int);
int write_file(vector<int>, string);